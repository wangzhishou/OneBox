工具发现：检索可用工具列表，动态扩展当前可用工具集。

使用场景：
- 当前工具列表中没有适合任务的专用工具时，先搜索再执行
- 传入 keywords 关键词数组, 关键词是你需要的工具名称、功能、用户意图等的关键词

传入 keywords 关键词数组，每个关键词独立模糊匹配。
返回：工具名 JSON 数组，如 ["pdf_to_images", "pdf_merge"]。
匹配的工具会自动加入下一轮可用工具列表（并集去重）。

> 注意：本工具用于搜索"可执行工具"（函数）。如需跳转应用页面，请使用 discover_apps。
