在一批文本文件中按关键词或正则表达式搜索内容，返回命中的文件、相对路径、行号和文本片段，适合代码 Agent 做跨文件定位。

输入支持本地路径、file:// URI，以及可解析为真实路径的 SAF content:// URI；工具会先统一转换为 file:// URI。

支持：
- 可选 glob 过滤（先筛文件，再搜索）
- 普通关键词搜索
- regex 搜索
- 大小写敏感控制

限制：
- 只搜索文本文件
- 无法解析为本地 file:// 的 URI 会直接报错

