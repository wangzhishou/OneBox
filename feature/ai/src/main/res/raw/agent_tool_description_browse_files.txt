只读浏览文件目录，列出子目录和文件信息；也可根据文件 URI 反查所在目录。传入的本地路径、file:// URI、可解析的 SAF content:// URI 都会先统一转换为 file:// URI。结果包含 deeplink，可用于跳转到文件浏览器查看。

使用场景：
- 查看某目录下有哪些文件
- 确认文件路径或 URI 是否存在
- 操作文件前先了解目录结构

注意：只做只读浏览，不支持删除/重命名/复制/移动。未传 directory_uri 时默认浏览主存储。对于无法解析成真实本地路径的 content:// URI，会直接返回错误而不是继续保留 SAF URI。
