按 glob 模式递归查找文件或目录，适合代码 Agent 在读取、搜索、编辑前先发现目标文件。

输入支持本地路径、file:// URI，以及可解析为真实路径的 SAF content:// URI；工具会先统一转换为 file:// URI。

典型场景：
- 查找所有 Kotlin / Gradle / XML 文件
- 在 feature 或 core 目录中发现某类命名文件
- 在补丁前先缩小目标文件范围

返回：
- 起始目录 file:// URI
- 匹配到的 file:// URI 列表
- 相对路径
- 是否被截断

