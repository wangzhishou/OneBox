读取本地文本文件内容并返回结构化 JSON 结果，适合代码 Agent 按行阅读与检索。输入支持本地路径、file:// URI，以及可解析为真实路径的 SAF content:// URI；工具会先统一转换为 file:// URI。

使用场景：
- 读取指定行范围并查看行号
- 在文件中搜索关键词并定位匹配行
- 拉取命中上下文，快速分析类、函数或配置片段

限制：
- 只支持文本文件，不处理二进制内容
- 无法解析为本地 file:// 的 URI 会直接返回错误
