根据文字描述生成 AI 图片(文生图),产物保存到 App 工作目录的 generated-images 文件夹,可在文件管理器中查看。

使用场景:
- 用户要求画一张图、生成插画/海报/封面等配图
- 需要为文章、笔记或卡片制作配图

注意:同一提示词默认复用缓存图片,force_refresh=true 可强制重新生成;size 可选(如 1024*1024),不填用模型默认尺寸。生成结果返回本地文件路径 filePath,可在回复中告知用户查看位置。
