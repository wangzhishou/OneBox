<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night-mode override: use a dark splash background so the splash
         screen doesn't flash white before the dark Compose surface appears. -->
    <style name="Theme.OneBox.Launcher" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FF1C1B1F</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen_animated_icon</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.OneBox</item>
    </style>
</resources>

